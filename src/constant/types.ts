export type Errors = {
    title?: string;
    description?: string;
    dueDate?: string;
  };

  export type TodoFormState = {
    title: string;
    description: string;
    dueDate: string;
  };
  