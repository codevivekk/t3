export type Errors = {
    title?: string;
    description?: string;
    dueDate?: Date | string ;
  };

  export type TodoFormState = {
    title: string;
    description: string;
    dueDate: Date | string ;
  };
  